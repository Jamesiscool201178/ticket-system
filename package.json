{
  "name": "ticket-system",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "node server.js",
    "build": "cd client && npm run build",
    "dev": "cd client && npm run dev",
    "dev:public": "cd client && npm run dev:public",
    "install-client": "cd client && npm install",
    "postinstall": "npm run install-client"
  },
  "dependencies": {
    "body-parser": "^1.20.4",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.2"
  },
  "devDependencies": {
    "nodemon": "^3.1.14"
  }
}
