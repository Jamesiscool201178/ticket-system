services:
  - type: web
    name: ticket-system
    env: node
    plan: free
    branch: main
    buildCommand: npm install && npm install && cd client && npm install && npm run build
    startCommand: npm start
    envVars: []
